import 'package:flutter/material.dart';

class AdminPanelPageSettings extends StatelessWidget {
  const AdminPanelPageSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
